SHORTCUT_TOKEN=your_shortcut_api_token_here

# MongoDB Configuration (optional - defaults to local MongoDB)
# For local MongoDB (default):
# MONGO_URI=mongodb://localhost:27017
# MONGO_DB_NAME=snip

# For MongoDB Atlas:
# MONGO_URI=mongodb+srv://<username>:<password>@<cluster>/?retryWrites=true&w=majority
# MONGO_DB_NAME=your_database_name

# AI Report Generation (optional - OpenAI API key is required per-user at runtime)
# Note: Users will be prompted to enter their OpenAI API key when generating reports.
# The key is not stored and is only used for the current report generation.
