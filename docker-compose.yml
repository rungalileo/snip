version: '3.8'

services:
  mongodb:
    image: mongo:latest
    container_name: snip-mongodb
    ports:
      - "27017:27017"
    volumes:
      - mongodb-data:/data/db
    environment:
      - MONGO_INITDB_DATABASE=snip
    restart: unless-stopped

volumes:
  mongodb-data:

